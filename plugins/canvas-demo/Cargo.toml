[package]
name = "canvas-demo"
version = "0.1.0"
edition = "2021"
description = "Reference WASM canvas plugin for agent-hand"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
strip = true

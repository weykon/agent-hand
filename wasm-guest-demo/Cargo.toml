[package]
name = "agent-hand-wasm-demo"
version = "0.1.0"
edition = "2021"
description = "Demo WASM canvas plugin for agent-hand"

[dependencies]
agent-hand-wasm-sdk = { path = "../wasm-sdk" }
serde_json = "1"

[lib]
crate-type = ["cdylib"]

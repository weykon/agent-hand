Build an interactive terminal canvas workflow editor replacing the right-side preview panel. Add petgraph 0.6 to Cargo.toml dependencies. Create src/ui/canvas/mod.rs with CanvasState struct holding a petgraph Graph of NodeData and EdgeData, positions as HashMap of NodeIndex to u16 u16 tuples, viewport as i32 i32 offset, selection as HashSet of NodeIndex, and a cursor_pos u16 u16. Create CanvasOp enum with variants AddNode RemoveNode UpdateNode AddEdge RemoveEdge Layout Batch Query, all serde Serialize Deserialize. Implement apply_op method on CanvasState for all variants. Create src/ui/canvas/render.rs with render_canvas function signature fn render_canvas f &mut Frame area Rect app &App that draws nodes as bordered boxes using Block and Paragraph widgets positioned via absolute Rect coordinates, and draws edges as line characters between connected nodes. Create src/ui/canvas/input.rs with handle_canvas_input that processes hjkl and arrow keys for cursor movement, Enter for node selection, Space for grab and drop repositioning, and returns bool for consumed events. Add canvas_state CanvasState field to App struct in src/ui/app/mod.rs and initialize with demo workflow of 3 nodes Start Process End connected by edges. Add pub mod canvas to src/ui/mod.rs. In src/ui/render/mod.rs change render_main to call render_canvas instead of render_preview for the right panel cols 1. Add Tab key handling in src/ui/input.rs to toggle focus between session list and canvas panel using a canvas_focused bool field on App. Support both Chinese and English labels matching the existing app.language pattern. Node kinds are process with normal border, decision with double border, start and end with rounded border. Viewport pans automatically when cursor moves beyond visible area. Simple grid auto-layout arranges nodes in rows when Layout op is applied. Keep render_preview available as fallback. Validate by running cargo build and cargo build --features max and cargo clippy all with zero errors. When canvas renders nodes and edges in right panel and Tab switches focus and hjkl moves cursor and Enter selects and Space grabs drops and CanvasOp is serde serializable and demo workflow displays on startup with no regressions output CANVAS_CORE_COMPLETE